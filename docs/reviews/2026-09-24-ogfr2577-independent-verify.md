# OGFR-2577 独立只读复核试演（2026-09-24）

## 范围与身份

用户在独立 Claude Code 会话中复核已归档的 OGFR-2577，并返回文字报告：命令操作约 1 分 23 秒，不含撰写报告；Token 用量未取得。维护者从业务仓库 `ac6520c55` 建立临时隔离副本，原始 PRD、Figma 引用及截图在试演前已复制到副本。副本与原业务仓库均未因此改动业务代码；原业务仓库另有一处无关的未提交代码修改，未进入副本。

复核报告未给出运行目录或文件摘要，因此不能证明它实际检查的是所准备的副本。它声称副本缺少原始来源，但准备好的副本中三份来源均存在；它还声称看到一份 9 月 18 日的质量报告，而该副本在复核时没有 `quality-report.json`。这两处与副本状态冲突，不能作为该副本的证据。复核结束后才将原业务仓库中 `tested_at=2026-09-21T05:53:29Z` 的本机质量报告补入副本，供后续复查；不能回算为本次 Agent 已读证据。

## 经维护者独立核对的发现

- R-2 替换的 9 张 `ic_cash_gift_chest_*` 是共享资源。除聊天气泡和封面预览外，`RedPacketOpenModal.kt`、发红包页 `RedPacketScreen.kt` 和 `RedPacketChestTierTablePreview.kt` 也通过 `closedChestRes`/相关资源访问它们。D-2 和 TEST-2 没有列出这些页面。开箱弹窗的 `CHEST_SIZE=250.dp`，新图在 xxhdpi 为 576px，显示到 250dp 时约放大 1.30 倍；这只是潜在清晰度风险，不是已观察到的视觉缺陷。原交付报告没有这些页面的真机验收证据。
- `RedPacketOpenModal.kt` 的箱体比例说明要求换素材后重测。独立复核报告称新旧图的宽度比例差异小于 1%；维护者未复跑该图像测量，也未找到当时交付的测量记录，因此不能据此认定位置、PAG 对位及清晰度全部通过。
- 代码有过期说明：气泡注释仍写 750×750 和 128dp，弹窗注释仍写 336×336。它们应在下次修改相关代码时校正，但不能仅凭注释判定现有运行行为错误。
- `traceability.json` 已保存 R-1→T-1、R-2→T-2；生成的 `traceability.md` 却显示 `?`。原因是 `render-workspace.py` 只读取旧的 `task_id`，没有读取当前的 `tasks` 数组。这是 Skill 生成视图缺陷，已在源码中修正并加入新旧格式回归测试；不需要修改 OGFR-2577 的 JSON 权威记录。
- 原业务仓库本机确有与报告时间一致的质量报告，记录 `compile-app-zymixtest` 和 `unit-docprep` 均为 passed；它在 `.gitignore` 下，不随 Git 克隆。即便补入副本，`unit-docprep` 仍不能证明本需求的 UI 行为。真机通过只有用户确认的文字记录，缺少可独立重看的截图或录屏；证据等级保持人工确认。

编号 OGFR-2577 在更早的设计计划中也出现过，但原始 PRD 只有一句尺寸修改；没有原始产品工单，无法判断是编号复用还是同一大需求的不同阶段，保持未裁决。

## 后续人工目视复核

2026-09-24，用户按追加的开箱弹窗、发红包页等共享素材检查清单完成查看，反馈“全部符合，我全部都查看过了”，未报告视觉异常。这降低了上述潜在放大、对位风险在所查看版本上的现实疑虑。证据类型为用户目视反馈；本轮没有取得装机包对应的 Git 修订、截图或录屏，因此不追认为 2026-09-21 原交付的版本绑定证据，也不抹去原 D-2/TEST-2 的影响面遗漏。

## 对多 Skill 方案的意义

独立复核确实提出了同 Agent 交付报告未覆盖的共享资源消费者，说明隔离 Verify 值得继续测；同时出现了与工作区事实冲突的来源/质量报告断言，说明独立上下文也会丢失交接事实。单次旧需求、无开发对照、未计完整耗时和 Token，不能推断准确率、节省成本或支持立即拆成六个 Skill。下一次试演应先固定复核目录、提交和本机证据清单，再比较同一需求的同 Agent 与独立 Verify 结果。
