# 已安装 Claude Code 0.5.20：Scope 与 Verify 隔离演练（2026-09-24）

本次观察真实 Claude Code 2.1.281 是否采用已安装 `/dev` Skill 的提示，不把脚本单独执行视为 Agent 行为。安装标记为 0.5.20；两次调用均在独立临时 Git 项目中运行，使用全新非持久会话。首次调用因本机未登录而在读取文件前停止，不纳入结果；登录成功后重跑。

## Scope：脱敏 Android 构建声明副本

隔离项目只含根 settings、49 份 Gradle 构建声明及一份被加载的 settings 脚本，不含业务源码。提示要求生成模块候选、调查完整性与人工复核点，禁止 Gradle、基线发布和业务修改。执行轨迹显示 Agent 读取已安装 Skill 的模块说明，调用 `module_context.py discover`，随后查看候选文件和配置缺口，并人工阅读根 settings、应用构建声明与被加载的 settings 脚本。

候选结果为 30 个静态模块、166 条候选边和 21 条缺口。Agent 明确指出根 settings 的外部脚本加载行与应用构建声明中的动态配置依赖行需人工复核；又从外部脚本核出 16 个动态声明的原生模块，说明它们没有被自动收录，并指出 React Native 自动链接、复合构建及缺失配置文件的边界。它没有把候选图说成完整依赖图，也没有把动态模块写进正式目录。

轨迹记录 7 次工具调用，约 67.1 秒，Claude CLI 报告费用约 0.585 美元。原有 52 个输入文件逐一 SHA-256 未变；Git 只出现新建的 `.agent-workflow/modules/candidates.json` 与生成视图。没有执行 Gradle 或完整 scan。本例证明 Agent 在明确的调查任务下实际读取并使用了 0.5.20 新增的行号提示；提示本身也要求说明完整性，因此不能据此推断所有普通用户表达都会自动触发同等调查。

## Verify：合成的缺证据 Feature

另一个隔离 Git 项目预置了索引声明存在、但在新提交中已删除的原始 PRD；一份未登记的补充文本；以及被 Git 忽略、测试版本早于当前 HEAD 的质量报告。提示只允许验收前只读核对，不运行构建或测试，不写记录。

执行轨迹显示 Agent 读取已安装的 Verify 流程说明，实际调用 `verify-inputs.py`，再结合文件和 Git 历史作判断。它明确区分脚本退出码 0 与清单状态 `review_required`，指出原始 PRD 不可用、补充文本未登记、需求与验收项尚未形成、质量报告版本不同且不能单靠旧单测证明交付；结论为证据不足，未声明验收通过。报告提及的其他缺口来自额外文件检查，不全由输入盘点脚本自动发现。

轨迹记录 4 次工具调用，约 30.5 秒，Claude CLI 报告费用约 0.286 美元。原有 5 个输入文件逐一 SHA-256 未变，Git 工作区保持干净。没有运行测试、构建、`validate --stage check` 或 `audit-delivery`，因此本次不评价完整 Verify 门禁。

## 结论与下一次验证

本次是已安装版的真实 Agent **隔离采用性演练**：Scope 实际使用 0.5.20 新增缺口提示，Verify 实际使用 0.5.19 引入且包含在 0.5.20 安装版中的只读输入核对。两个项目都为人工设计的隔离样本，输入不满足真实需求、完整业务源码、同条件 A/B 或端到端交付的前提；不能据此声称推荐准确率、误报率、上下文收益或自主交付率已得到验证。下一真实跨模块需求仍需记录自然触发、遗漏、误报、人工补查与总耗时。独立 Verify 是否值得拆分，仍按对照协议取同条件双组证据。

完整执行轨迹和原始隔离样本留在本机临时目录，不进入仓库；本文件只保存脱敏结论与可复核的计数。临时轨迹 SHA-256：Scope `ac0040a444cad373d26b394492c7d0aa59c7b5e8eeabaf9cfe7df3eab922ffdf`，Verify `02d112fa02168aaabbd0294811aa7e2bbb1552564c1793f056a9e8855ce02530`。
