# 验证与评估记录

## 0.5.2 — 2026-09-20

本次仅增加 Agent help 路由和帮助说明，未改执行脚本。核对主命令表全部 16 项在帮助中可查、单命令参数/区别、相对链接、版本和 diff 空白；未重跑未变化的 142 项脚本测试，未宣称真实 Claude 会话验收。帮助明确优先返回、不要求项目或当前需求、不执行示例。通用 quick_validate 因缺少 PyYAML 无法执行，基本 frontmatter 另行检查。

## 0.5.1 — 2026-09-20

完整合成回归 142/142 通过（14.251 秒，新增 10 项）。临时 Git 项目验证旧需求兼容/采用后缺失阻断、未决项分阶段阻断、修改与保留行为均需回归、共享文件变化及范围外新增检测、重命名两端、清单语义变化失效、工作区进度不失效、明确排除/豁免、错误类型/路径/符号链接拒绝、实际校验/渲染入口，以及内容不变提交保留证据摘要。

测试证明记录与范围检查，不证明 Agent 调查完整、回归实际执行或业务语义正确。未改业务项目，未安装到 Claude，未做真实局部需求评估。下一步采集一次实际修改的遗漏捕获、填写时间与误阻塞情况。

Skill 通用 quick_validate 本次仍因缺少 PyYAML 无法运行；不计为通过。另做 Python 语法、相对链接与 diff 空白检查。

## 0.5.0 — 2026-09-20

完整合成回归 132/132 通过（最终重跑 12.994 秒，新增 16 项）。覆盖基线采用、原语义快照保留、不虚构交付版本、提案不改变当前规格/去重、未批准决策拒绝应用、版本/原值冲突拒绝、取消保留历史、显式新增/废弃需求、直接语义漂移及已应用提案改写检测、任务关联变化不推进语义版本、原文/补充来源字节保护、旧或未绑定检查报告拒绝、修订测试漏选和错误代码版本拒绝、版本化验收不自动 complete、原文覆盖明确取代及检查运行中需求漂移拒绝。

中途完整回归发现独立导入扫描模块时的依赖定位问题，以及 macOS 临时目录别名在新增来源路径校验中的规范化问题；已修正并由上述最终全量回归覆盖。测试为隔离合成资料与真实临时 Git 仓库，质量命令执行简单确定性断言；未修改业务项目，也未跑真实 Android 构建或真实 Agent 修订评估。不能把结构检查解释成语义判断、完整影响分析或真实 UI 验收。

quick_validate.py 仍因本机缺少 PyYAML 未能执行；另检查改动 Python/JSON 语法、Markdown 本地链接、frontmatter 基本字段和 diff 空白错误。前置维护文档已提交 d6433ba；0.5.0 的提交、推送和个人安装状态需分别按 Git 与安装标记核对。下一验证为一次实际小修订：核对原文保留、确认范围、任务/代码/测试落实及记录成本。

## 阶段口径同步 — 2026-09-20

依据本页 CASE-01 最终收尾及表达差异评估，维护文档明确第一阶段已完成，剩余问题转持续维护、第二阶段尚未开始实现。此次仅修改维护说明与接续规则，不改运行代码或版本；未重跑未变化的 116 项测试，也未新增业务真机验证。早期条目保留当时结论，应按日期与后续收尾记录理解。

## CASE-01 首个真实交付闭环 — 2026-09-18 最终收尾

维护者只读核对业务仓库：最终代码提交 `14bc14fac`，记录提交 `1cc96db99`，后者仅修改需求工作区记录。需求状态 complete，4 项有效需求 confirmed、4 项任务 done；FIX-001～006 verified，FIX-007 closed。诊断日志清理后的导航文件相对功能修复基线 `fca2e1560` 无差异。

执行方交付报告记录：完整 V2 12 项验收基于诊断包 `ae1577b3b`；最终代码 `14bc14fac` 有独立 M2/M12 真机冒烟和表单保留目视结果。编译、定向单测、结构 check 与交付 audit 均报告在该最终代码版本通过；不把旧包日志称为新包全量验收。维护者本轮核对提交、记录和代码差异，没有重跑构建、装机或真机操作。记录提交位于被测代码提交之后，不宣称此前 audit 自动覆盖后续 HEAD。

证据边界：M3 为用户目视；FIX-005 有自动化回归 waiver，后续改动表单持久化时须人工重验 M12。FIX-007 原始异常观察保留、未归因；当前设计行为经实测支持关闭，复发需新建关联记录。业务报告末尾还保留清理前边界文字，最新完成章节与结构记录为本次结论，旧措辞宜整理为历史备注。

结论：首个真实需求最终交付闭环和一次 8/8 表达差异评估均有结果，第一阶段基础交付验证里程碑完成。并不代表后续新版本的所有入口已通过真实 Agent 评估，也不自动关闭 GAP-03～08 的剩余能力或成本问题。原始业务资料与日志留在所有者项目，本仓库仅保存脱敏结论。

## 0.4.11 — 2026-09-18

完整合成回归 116/116 通过（最终重跑 8.935 秒，新增 9 项）。覆盖归档原地保存来源/报告/状态、默认隐藏与历史筛选、多模块标签与清空、PRD 语义摘要不变、幂等归档/恢复与事件历史、不完整需求/任务/资料拒绝、未决决策及未关闭修复拒绝、缺失/空/越界证据拒绝、归档后的写入及 develop/check 阻断、只读查看/渲染、恢复后 fix 转 provisional、坏元数据和符号链接保护。

仅用合成资料；未归档真实业务项目，未验证 Agent 对交付报告真实性的判断或在真实会话中的恢复衔接。归档检查结构与已登记事实，不代表重跑验收；无全局多文件事务保证。测试执行时尚未安装、提交或推送本版本；后续发布以 Git 核对。quick_validate.py 因缺少 PyYAML 未运行；另行检查改动的 Python/JSON 语法、本地 Markdown 链接、frontmatter 基本字段与 diff 空白错误，均通过。

## 0.4.10 — 2026-09-18

完整合成回归 107/107 通过（7.746 秒，新增 8 项）。验证只读列表/选择、显式覆盖不更改默认、分别传入的会话选择不互相写入、跨项目拒绝、非法/丢失/损坏/越界记录拒绝、已有 ID 与失败初始化不覆盖旧记录。选择由 Agent 会话保存，测试不证明主机多会话执行或 Agent 自动选中行为；新建完成后选择与失败保持由运行指令约定，尚待真实 Claude/Codex 使用验证。

quick_validate.py 因本机缺 PyYAML 未能执行；另检查 frontmatter 基本字段、Python 语法、新增引用路径及 git diff 空白错误。未安装、提交或推送；保留此前 0.4.9 工作。

## 0.4.9 — 2026-09-18

完整合成回归 99/99 通过（8.70 秒，新增 4 项）。验证 draft 局部评审可记录 pending 来源条目和 unreadable 单元，保留原文状态；默认完整复核及 develop/check 仍严格阻断；draft digest 无法冒充完整复核；坏来源路径仍拒绝。新增门禁修正尚未经过真实 Agent 使用，也未安装到个人目录。独立表达差异评估使用的是 0.4.8，不能当作 0.4.9 的行为评估。

## PRD 表达差异样例评估 — 2026-09-18

执行方独立样例运行报告为 8/8 语义通过，禁止项零命中；仅一模型一次运行。结果、工件状态与发现的 draft 评审摩擦见 [评估归档](evaluations/2026-09-18-prd-styles.md)。原件为本机临时资料，已另存给所有者；本仓库只存无个人路径的摘要。真实业务最终交付仍待真机验收。

## 0.4.8 — 2026-09-18

完整合成回归 95/95 通过（8.43 秒，新增 11 项）。覆盖澄清登记/去重/编号、原规格保留、旧决策兼容、待决阻断、complete 转 provisional、确认后未落实阻止 check、已有规则无需重新确认、变更确认及落实证据缺失拒绝、替代决策引用、损坏记录拒绝覆盖，以及状态/渲染输出。输入为隔离合成资料，不触及业务工作区。

skill-creator 的 quick_validate.py 因本机缺少 PyYAML 未能运行；另行检查 frontmatter、Python 语法、文档本地链接及 diff 空白错误。未验证 Claude/Codex 在真实对话中的自动识别、调查质量或新入口发现；此节记录 0.4.8 源码测试当时的状态；后续安装与独立表达评估另见本页新条目。第一阶段真实业务最终验收仍未完成。

## 0.4.7 — 2026-09-18

完整合成回归 84/84 通过（约 7.23 秒，新增 5 项）。新增合成场景验证已通过检查的测试选择、旧选择漏测、项目快照变化、失败检查不能算通过以及无效测试 ID 元数据拒绝。仍未在真实业务项目执行，不能证明 Agent 标注的 ID 对应命令实际运行的断言。

## 0.4.6 — 2026-09-18

- 本轮完整合成回归 79/79 通过（约 6.31 秒），其中修复流程 20 项；新增 10 项测试。GAP-01/02 新增覆盖：跨范围测试拒绝/解释、任务关联测试、无效说明及未知测试、非缺陷关闭、证据缺失、撤回确认、重复原问题阻断、自引用/重复链、再次报告及不自动恢复 complete。
- skill-creator 的 quick_validate.py 因本机 Python 缺少 PyYAML 未运行成功；已人工核对未改动的 frontmatter，并检查运行资源和维护文档的本地链接。
- 所有新增样例为合成资料，未更新个人安装或业务工作区；未完成真实案例运行时验收，不证明关闭判断或跨范围覆盖说明的语义正确性。在 0.4.6 时，GAP-03 至 GAP-08 仍未实现。

## 真实验收包校对 — 2026-09-17 执行，2026-09-18 脱敏归档

案例 CASE-01 的范围及事实见 [校对记录](reviews/2026-09-18-case-01.md)。维护者确认原始 HTML 与来源副本字节一致，安装标记 0.4.5 的 34 个登记文件与标记及源码一致；在临时目录对验收记录运行 develop/check，均返回 FEATURE_VALID。

上述结构通过时仍发现：任务/测试计划与已批准默认值相反、已确认行为仍被写为假定、功能状态与报告不同步、正式检查过滤器未选择后续新增回归、最终报告停留在早期代码。部分测试结论覆盖了断言并未验证的导航或副作用。这些分别支持 GAP-03/04/07/08 的后续修正，不证明已修复。

业务执行方报告的历史编译/单测结果仅作所提供证据；维护者本轮未重跑业务构建、真机或动态交互，也未确认远程开关后台。已取得真实使用反馈，但完整交付与 PRD 表达差异评估仍未完成。2026-09-18 只修改维护文档，未重跑无变化的 69 项合成回归，不能把历史结果计为今日新测试。

## 阶段审查 — 2026-09-17，源码 bd0cfc8 / 0.4.5

本轮审查已重新执行 `python3 -m unittest discover -s tests -v`：69/69 通过，约 6.17 秒。随后使用临时功能、临时代码和临时 Git 仓库做额外检查，未访问业务资料。此次维护文档整理引用这次真实运行，未在文档改动后再次执行未变化的运行测试。

| 对应问题 | 隔离检查结果 | 证据边界 |
|---|---|---|
| GAP-01 | 修复关联 R-1，却引用仅属 R-OTHER 的 UT-OTHER，修复校验零错误 | 证明缺少覆盖关系校验；不代表所有跨需求测试都不合理 |
| GAP-02 | 支持的关闭状态只有 verified；尝试 dismissed 被判为 invalid status | 证明没有专用非缺陷处置路径；具体新状态尚未设计 |
| GAP-03 | 将示例代码从大写转换改回小写后，历史修复仍通过结构 check | 未执行实际业务回归；不声称完整 /dev check 必然漏掉缺陷 |
| GAP-04 | 子任务标题改成与需求相反的含义，develop 校验仍通过 | 当前 PRD 复核不覆盖任务内容；不代表能自动判定所有语义冲突 |
| GAP-05 | 未登记 Kotlin 源码的未提交修改不改变指纹；无关 README 提交只改变 git_head | 基线有效不等于全部源码未变；尚无实际重复扫描耗时数据 |
| GAP-06 | 从规则与文档确认功能级阻塞，未测量实际额外成本 | 属待验证风险，不是已证实过度设计 |

详细复现与代码定位见 [审查报告](reviews/2026-09-17.md)。额外检查属于一次性审查实验，尚未加入 69 项常规测试。当前处理状态统一见 [路线图](../ROADMAP.md)；上述问题本次未修复。未进行真实 Android 构建、完整动态 HTML 交付或独立上下文 PRD 语义评估。

## 0.4.5 — 2026-09-17

- 69/69 合成回归通过（0.4.5 功能提交前执行；后续阶段审查的重跑另见上节）。新增覆盖：仅描述登记、重复登记去重、再次出现新编号、complete 回到 provisional、修复 Markdown 生成、旧工作区兼容、未验证问题阻止 check、普通代码 bug 不改需求、需求变更需要已确认决策、证据与引用缺失拒绝；新增返回上一页的双重成因、任务描述前后变化及回归测试缺失/无效引用门禁，以及明确豁免路径。
- 未在真实新功能中评估 Agent 根因分类、用户验收或修复后的实际效果；字段与测试证据校验不证明结论真实。

## 0.4.4 — 2026-09-17

- 59/59 合成回归测试通过；覆盖文档单独、HTML 单独、两者组合、未覆盖来源、交互观察字段、静态 HTML 范围说明、完整联合来源复核与未决冲突门禁、来源字节/登记变化使复核摘要失效、重复或缺失输入不留工作区。
- 未对真实动态 HTML 执行浏览器遍历，也未评估跨来源语义冲突识别准确率。下一轮真实验证须分别记录文档、HTML、组合输入的来源覆盖与无法触达状态。

## 0.4.3 — 2026-09-17

- 51/51 合成回归测试通过。新增真实临时 Git 仓库测试：忽略范围/逐项例外、重复执行保留规则、已跟踪文件仅提示、父级忽略与模板冲突、新克隆重建本机状态且保留分析与干净工作区。
- 未上传真实资料，未改变业务仓库索引；忽略模板不是秘密检测工具，无法保证规格/摘录不含敏感内容。

## 0.4.2 扫描保留验证

合成项目覆盖无变化不重写、3 份完整历史、固定快照/自定义数量、草稿发布、非法配置保留旧版、并发变化拒绝、替换失败回退、中断恢复及符号链接拒绝。未运行真实 Android 构建，未证明语义覆盖准确率或断电持久性。

本次 macOS/Python 3.9 回归：47/47 通过。Windows 锁分支尚未在 Windows 主机验证。


## 0.4.1 — 2026-09-17

- 39/39 自动测试通过，含原有 29 项回归。
- 新增覆盖：纯校验不执行、后续配置错误阻止所有命令、条件/模板拒绝、空选择、候选不执行、仅执行已选项、cwd/重复名/超时校验、登记证据失效、越界拒绝、保留覆盖记录。
- 首次真实项目仅做基线修正与配置验证；未运行 Android 构建或测试。
- 仍未实现完整增量扫描或任务级变化传播。

## 0.4.0 — 2026-09-16

- Python 3.9.6：29/29 自动测试通过；包括原有安装/迁移/质量检查回归。
- 新增：读取缺口、空清单、旧工作区缺 intake、来源越界、排除规则、双向关联、条件与例外、复核失效、任务进度不使复核失效、嵌套错误类型、最小语义改动的摘要变化。
- Python AST、JSON 解析、shell 语法通过。
- 多表达 PRD 样例与人工预期已整理；未执行跨模型独立 Agent 行为评估。摘要变化测试不证明语义理解能力。
- 未验证真实 Android 构建、复杂 Word/PDF/OCR 和 UI 内技能发现。
- 官方 quick_validate.py 在本机缺 PyYAML，之前未能运行；本次未将其记为通过。

## 0.3.0

原 13 项隔离回归测试通过。详情由 Git 历史保留。


## 局部改动影响面验证（待执行）

真实新需求若仅修改既有模块的一部分，记录旧行为、新要求、明确保留的行为和未决歧义，附上来源或用户决策；核对入口、调用方及共享路径，把受影响任务、代码、测试与改动范围外的回归场景一起留证。执行前后比较实际差异，记录范围扩大原因、相关回归结果与未覆盖风险。CASE-01 已完成记录/代码回顾与最终运行时验收；上述开发前影响面流程的独立试行仍待进行，也未实现自动影响面门禁。

## 下一轮真实反馈记录

记录日期、源码/安装版本、Agent/模型、执行命令与阶段、耗时、提问/报错、预期与实际差异、最小脱敏证据。scan 额外记录覆盖范围、是否变化、是否新增备份、草稿/发布状态；PRD 额外记录遗漏/误解的原文条目、条件/例外、受影响需求及人工修订。另记录填写/复核耗时、重复录入、误阻塞次数、修订同步成本，以及流程实际捕获的偏差；未采集的数据写“未采集”，不能补推成测量结果。

此前真实反馈为首次 scan 约 3 分钟、用户未发现明显错误；后续审计发现配置与基线描述问题并修正。此反馈不证明扫描准确率、后续扫描耗时或重复读取已消除。0.4.2 的备份机制目前以合成回归验证为依据，CASE-01 的早期校对当时尚未闭合；后续最终验收已完成，见本页最终收尾记录。

2026-09-17 早期维护文档同步时未重跑代码测试；该记录引用当时的 47/47。后续 0.4.4 功能提交前已运行 59/59 合成回归；0.4.5 功能提交前及本页上方阶段审查均运行了 69/69。这些合成测试不替代真实场景验证。
