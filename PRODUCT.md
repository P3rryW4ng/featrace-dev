# FeatraceDev 产品方向基线

状态：**已确认方向基线**  
日期：2026-09-23

本文件只定义产品为什么存在、最终要交付什么和如何判断方向是否前进。它不保存版本待办、实现细节或历史验证结果。产品所有者的新确认高于本文件；确认后应更新这里，再由架构决策、路线图和运行实现承接。

品牌：**FeatraceDev**；仓库：`featrace-dev`；作者展示：**Created by Perry Wang**。Feature · Trace · Delivery 是产品理念，Dev 表示开发。品牌调整不改变下述北极星与阶段目标。

## 当前阶段目标

把开发者使用 Coding Agent 修改已有项目的过程变得更高效、透明和可控：降低 PRD 频繁变化造成的返工，帮助开发者理解相关代码与他人留下的逻辑，约束新功能对旧行为的影响，并用真实测试与交付证据避免黑盒开发。

当前阶段增强开发者的判断和交付能力，不以取代开发者为目标。需求权威、重要产品取舍、主观体验验收和不可逆发布仍由人掌握。

## 战略北极星

在已有代码库中，将持续变化的产品意图转化为可验证、可交付、可接续的软件变更，并在未来修改中保护已经确认的行为。

## 最终愿景

给定 PRD 和已有代码库，系统在明确边界内自主完成需求理解、影响调查、任务拆解、编码、测试、修复与交付；只有遇到产品决策、缺少资料或权限、以及无法自动验证的行为时，才请求人介入，并保存可追溯的交付证据。

目标不是承诺任意 PRD 都能无人值守完成，而是让可安全自动完成的步骤持续推进，把真正需要人的选择集中暴露出来，不把猜测或结构检查伪装成可交付成品。

当前阶段目标、战略北极星和最终愿景是递进关系，不是三个互相竞争的方向：先建立透明、可追溯的辅助开发，再形成受控编排，最后提高有边界的自主交付比例。每一级都必须通过真实需求证明收益，不能为了自动化而削弱证据、产品权威或人工接管能力。

| 成熟阶段 | 目标 | 人的主要职责 |
|---|---|---|
| L1 透明辅助开发 | 需求、影响、任务、修复和验证可追溯 | 决策、开发监督与验收 |
| L2 受控流程编排 | 系统自动选择并连续执行安全步骤 | 处理明确暴露的阻塞与选择 |
| L3 有边界自主交付 | 适用需求可推进到可验收成品 | 产品权威、例外和主观验收 |
| L4 高度自动化交付 | 更多项目适配器和验证能力支撑端到端交付 | 治理、风险和不可逆发布 |

## 首要用户与场景

首要用户是使用 Claude Code、Codex 等 Coding Agent 修改已有代码库的个人工程师和小型开发团队，尤其适合：

- PRD 会补充、澄清或反复修订；
- 一个功能跨页面、状态、模块、灰度开关或外部资料；
- 局部修改需要保护未授权改变的旧行为；
- 历史 Bug 与对应回归需要在后续修改中重新考虑；
- 工作会跨会话、跨 Agent、跨机器或跨开发者接续；
- 构建成功之外仍存在真机、视觉、账号或业务验收。

一次性原型、数小时后废弃的 Demo 和不需要保留交付依据的简单修改不是优先场景；流程成本不能高于它们的实际风险。

## 要解决的核心问题

1. **交付事实分散**：PRD、设计、口头确认、代码、测试和真机结果无法在换会话后可靠恢复。
2. **需求随时间变化**：新说明可能是澄清、修订、误读纠正或代码缺陷，不能只按时间覆盖旧事实。
3. **已有项目影响难查**：局部改动可能触及共享状态、导航、生命周期、调用方和历史修复。
4. **完成声明缺少证据**：编译、单测、人工目视和真机验收的证明范围不同，旧证据也可能因新改动失效。
5. **历史没有参与未来开发**：归档需求和已修复问题没有在相关代码再次变化时提供保护。

## 用户最终应得到什么

输入可以是一份或多份 PRD、设计、API、交互资料及已有代码库。最终输出应包括：

- 可审阅的需求理解、待决问题和已确认边界；
- 与需求对应的实现代码；
- 实际执行过的自动测试和仍需人工完成的验收；
- 对失败的调查、修复和重新验证；
- 绑定当前需求与代码版本的交付结论；
- 能让下一次相关开发恢复上下文并保护历史行为的记录；
- 在项目具备条件时，可进一步形成提交、PR、安装包或其他明确交付物。

## 产品结构

### 1. 自主交付引擎

负责从当前状态选择下一步，并连续推进可以安全执行的工作：

```text
PRD → 分析 → 必要澄清 → 项目调查 → 任务规划
    → 编码 → 测试 → 失败修复 → 重新测试 → 验收 → 交付
```

它必须有明确停止条件：需要产品裁决、资料或权限缺失、风险超出授权、以及只能由人判断的验收。`next` 和会话模式是当前入口雏形，不等于完整自主循环已经实现。

### 2. 事实与证据内核

负责保存原始来源、当前需求基线、修订、任务、决策、影响范围、Fix、测试证据、交付状态和历史回归。它约束自主引擎不能静默改写产品意图、继承过期结论或把未验证状态说成完成。

### 3. 项目适配器

负责把通用流程接入具体技术栈和外部系统，例如 Android、Web、iOS、后端、Figma、OpenAPI、模拟器、真机、GitHub 与 CI。适配器提供项目事实和执行能力，不改变产品权威关系。

用户只需要一个主要产品入口。内部流程可以模块化并按需加载；复杂精确命令用于高级控制和排障，不应成为普通用户必须记忆的主流程。

### 上下文与专业能力边界

产品采用“一个用户入口、小型路由器、专业能力、共享事实与证据”的结构，避免把完整交付规则长期塞入同一上下文。专业能力按独立产物与责任拆分，而不是按命令数量拆分：

- Requirement：来源读取、需求基线、澄清与修订；
- Scope：项目调查、模块职责、影响面、任务和历史修复候选；
- Build：按已确认任务修改代码并登记实际范围；
- Verify：独立核对需求、差异、测试和人工验收缺口；
- Repair：对失败调查、分类、修复并交回 Verify；
- Deliver：核对证据，形成报告、提交/PR 候选与归档结果。

路由器只读取当前 Feature 状态和各阶段的精简结果，优先使用确定性状态决定下一阶段，不重新分析所有原文和代码。阶段事实写入共享记录，并用输入摘要、证据引用、实际产物和允许的下一步形成结构化交接；自然语言总结不能单独充当阶段完成依据。

拆分分级实施：先在同一 Agent 中缩小主 Skill 并按需加载专业说明；取得上下文与成本数据后，优先让 Verify 使用独立 Agent/上下文，再评估 Requirement、Scope 和任务级 Build。多个文件不等于上下文隔离，多 Agent 也不自动更准确；重复读取、交接损失、冲突与 Token/时间成本必须纳入验证。未经对比评估，不把每个命令拆成一个 Skill 或 Agent。

## 当前完成边界

第一阶段已经建立可追溯交付底座，并取得真实 Android 需求、PRD 表达样例和多项隔离演练证据。当前强项集中在事实与证据内核；自主交付引擎和项目适配器仍不完整。

| 能力 | 当前成熟度 | 当前边界 |
|---|---|---|
| 多来源 PRD 读取与需求基线 | 真实案例 | 不能保证自动发现全部遗漏或动态交互 |
| 澄清、修订、Fix 与决策留痕 | 真实案例／结构验证 | 原因与产品含义仍需 Agent 和人判断 |
| 任务、追溯和任务语义复核 | 隔离验证 | 自动拆解准确率与真实维护成本待验证 |
| 模块、能力职责和影响面 | 已实现／隔离验证 | Android Gradle 候选尚待真实项目验证，动态关系不完整 |
| 自动编码编排 | 初步可用 | 依赖 Agent，会话入口尚未形成可度量的完整循环 |
| 自动检查和证据新鲜度 | 真实案例 | 自动选择测试及行为覆盖仍需人工核对 |
| 失败诊断、修复和重测 | 部分真实案例 | 有记录流程，尚无稳定的自主修复循环 |
| 真机、视觉和业务验收 | 人工流程 | 尚无通用设备驱动和视觉自动判定 |
| 历史修复回归保护 | 隔离验证 | 真实项目误报、漏报与成本待测量 |
| 提交、PR、安装包和发布 | 未形成统一闭环 | 当前以代码、报告和归档为主 |

成熟度不能因增加字段、生成文件或合成测试通过而升级；真实能力以 [验证记录](docs/validation.md) 和脱敏案例为依据。

## 下一阶段唯一主目标

在真实需求中验证一条 **PRD 到实现和自动测试** 的连续自主链。优先减少不必要的人类操作，不继续用增加记录数量代替自主交付能力。

每次验证至少记录：

- 总耗时与 Skill 记录维护耗时；
- 自动完成的步骤及中断位置；
- 人类介入次数，以及介入是否真的需要产品或环境权威；
- 测试失败后是否能调查、修复并重跑；
- 误阻塞、漏报、返工和错误完成声明；
- 最终产物是否可构建、可测试、可验收和可接续。

## 成功指标

- **适用需求自主完成率**：满足资料、环境和权限前提的需求中，自动推进到预定交付点的比例。
- **必要与非必要介入次数**：区分真实产品决策和本可自行调查的问题。
- **端到端交付时间**：同时记录开发时间和流程维护时间。
- **捕获偏差与漏出偏差**：Skill 提前发现的问题，以及最终验收才发现的问题。
- **误阻塞率**：门禁阻止了实际无需阻止的工作次数。
- **历史回归覆盖**：相关旧修复是否被正确发现、重验或有据排除。
- **接续恢复成本**：新会话、账号或 Agent 恢复到可继续工作所需的时间和补充信息。

没有真实数据时保持“未测量”，不使用主观估算伪造准确率或效率提升。

## 非目标与约束

- 不宣称自动理解完整代码库或保证 PRD 零漏读；
- 不用结构校验、编译成功或测试 ID 非空代替业务正确性；
- 不替代产品权威、代码审查、CI 和必要的人工验收；
- 不为了显得自动化而替用户做未获授权的产品选择；
- 不把命令数量、JSON 数量、测试数量或 GitHub Star 当作核心产品结果；
- 不同时扩展所有平台，先用真实项目证明一个适配器的价值与成本。

## 防止产品开发漂移

开始实现新能力前，必须明确回答：

1. 它解决了哪个真实交付问题，有什么案例或证据？
2. 它属于自主交付引擎、事实与证据内核还是项目适配器？
3. 它提高自主完成比例、降低错误，还是只增加记录？
4. 不实现它，真实需求会在哪一步失败？
5. 用户需要增加多少输入、等待和复核成本？
6. 用什么真实或隔离场景证明它有效，又如何判断误阻塞？

方向改变先更新本文件并由产品所有者确认；架构取舍进入 `docs/decisions/architecture.md`，阶段和 GAP 状态进入 `ROADMAP.md`，执行证据进入 `docs/validation.md`。聊天记录、安装副本和 Agent 自述都不是产品方向权威。
