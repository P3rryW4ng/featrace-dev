# Changelog

## 0.5.21 — 2026-09-24

- 修复 Gradle 候选中存在未被静态 include 的依赖目标时，正式模块图拒绝整份候选、丢失所有候选边与具体缺口的问题。
- 未知目标仅以待审节点和虚线候选边显示，保留来源行号与缺口；不写入正式模块目录，也不自动确认动态模块。损坏或过期候选仍被拒绝。
- 对可识别的变体配置 `add("${flavor}Implementation", project(":module"))` 保留条件候选和行号；若正式目录把这条仅条件生效的关系写为无条件 `depends_on`，关系图与扫描发布会拒绝，要求人工纠正。未解析的 Gradle 表达式仍需人工调查。

## 0.5.20 — 2026-09-24

- Android Gradle 静态候选发现遇到外部 settings `apply(from=...)` 和 `add(..., project(...))` 时，列出文件与行号供人工复核；不执行外部脚本、不自动登记动态模块，也不把动态依赖当作已确认关系。
- 根据真实 Android 构建声明的隔离核对补齐两种静默遗漏提示；正式模块目录及业务项目不改动。

## 0.5.19 — 2026-09-24

- Verify 开始前增加只读输入核对：显示来源文件与补充资料的实际可读性和摘要、当前 Git 工作树、已有质量报告的被测版本；兼容旧版 intake 的来源单元索引。不新增用户命令或验收门禁，也不把旧报告当作通过。
- 校正 README、路线图与维护记录中已过时的当前版本措辞；保留各历史版本的原始验证结论。

## 0.5.18 — 2026-09-24

- 修复生成的 `traceability.md` 对当前 `tasks` 数组显示 `?` 的问题，同时保留旧 `task_id` 记录的显示；JSON 权威记录和门禁不变。

## 0.5.17 — 2026-09-23

- 确认当前开发者增强目标、持续变化需求的战略北极星与有边界自主交付愿景是递进关系，并以 PRODUCT.md 作为方向权威。
- 保留单一 `/dev` 入口，将现有流程组织为 Requirement、Scope、Build、Verify、Repair、Deliver 六个内部阶段；每阶段定义输入、产物、退出条件和按需加载规则。
- 阶段交接复用已有权威记录和摘要，不新增第二套状态或通用交接文件；本版仍为同一 Agent 的渐进加载，不宣称已启用多 Skill 独立上下文或多 Agent。
- 主 Skill 入口由约 2613 词缩至 721 词；初始化、来源适用性、迁移和完成条件转入对应阶段参考文件，保持现有命令及门禁约束。

## 0.5.16 — 2026-09-23

- Android scan 从常见 `settings.gradle(.kts)` include、`projectDir` 与模块 `build.gradle(.kts)` project 依赖生成 `modules/candidates.json` 和可读候选图；每条关系保留文件与行号依据。
- 候选层固定标记为 `candidate_only` / `pending_review`，不会自动写入正式 `modules/index.json`，不能满足模块职责、档案或开发门禁；动态 Gradle 表达式和缺失构建文件登记为解析缺口。
- 正式模块图在存在候选时用虚线展示静态 Gradle 关系，并继续用实线区分人工声明依赖与已确认业务职责；Generic 项目和旧记录保持原行为。

## 0.5.15 — 2026-09-22

- 新需求必须使用用户提供的产品/work-item ID；workflow v3 在 develop/check 前要求确认业务能力与模块职责，或有具体理由地标记不适用。
- 模块分类从单一标签扩展为 capability + 多模块角色：owner、host、provider、consumer、shared；每项保存职责和依据，同时保留旧记录兼容。
- 新增只读模块候选、确定性范围写入与门禁；路径匹配只作为候选，不自动裁决产品归属。
- scan 发布及范围确认会生成 `graph.json` 和 Mermaid `graph.md`，展示声明依赖与已确认能力关系，并明确列出旧标签和运行时动态关系等缺口。

## 0.5.14 — 2026-09-22

- `project.py verify` 区分 Git HEAD、构建清单、登记证据和工作树变化，并列出最多 50 个相关路径；未提交或未跟踪的非 workflow 文件不再被静默忽略。
- 仅 `.agent-workflow/**` 提交推进 HEAD 时保持基线有效；其他 HEAD 变化仍需审阅具体路径，不删除保守的 HEAD 检查。
- 经 scan 审阅发布后，若基线正文和除 HEAD 外的指纹完全不变，只刷新指纹且不生成冗余备份；清单、登记证据或说明变化仍正常备份。

## 0.5.13 — 2026-09-22

- 增加任务语义复核：任务标题、描述、范围、依赖、关联需求及未知扩展字段变化后，develop/check 要求只复核受影响任务和新旧关联需求。
- 任务状态、测试/执行证据、验证结果、实现版本、时间和负责人属于进度字段，不使语义复核过期，也不要求重读整份 PRD。
- 新工作区默认采用 task-review.json；旧工作区在首次语义任务修改前按需采用。复核保存摘要、依据和防篡改历史，但不声称摘要能判断文字是否正确。

## 0.5.12 — 2026-09-22

- 明确回归候选同步只负责发现：用户要求只同步、列出或停止时，Agent 必须保留 pending，不得自行写入 retest 或 not_applicable。
- regression-review.json 升级到 schema v2；候选变化或移除时，将旧 disposition、result 和候选快照移入只读 history，而不是删除。
- superseded history 不满足当前 develop/check 门禁；同步幂等，状态摘要和归档报告显示历史数量，v1 记录可继续读取并在下次同步时迁移。

## 0.5.11 — 2026-09-21

- develop/fix/revise 实现前与 check 前增加历史修复回归提醒，不新增用户命令；按已登记模块和代码路径查找当前及其他需求中的 verified FIX。
- 新增 regression-review.json：候选必须选择 retest 或 not_applicable；重测在 check 时需要当前版本、方法和证据，豁免需要剩余风险与批准依据。
- 候选、当前范围或历史修复证据变化会使记录/结果过期；旧 FIX 不被自动重开，缺少模块/路径登记的内容也不宣称自动发现。

## 0.5.10 — 2026-09-21

- `/dev archive` 增加归档预检：从现有需求、任务、决策、追溯、修复和质量记录生成待审交付报告草稿，不虚构新验收结论。
- 预检报告模块目录候选、代码路径根、来源文件的 tracked/ignored/untracked/missing 状态，以及结构校验、audit 和 impact 是否存在。
- 已有非空交付报告不会覆盖；自动草稿带 review-required 标记，Agent 未审阅并移除标记前正式归档会被拒绝。

## 0.5.9 — 2026-09-21

- 修正质量证据闭环：质量检查后只提交 `.agent-workflow/**` 记录时，audit 不再因 Git HEAD 前移而误判过期；同一提交包含业务/配置文件变化时仍会过期。
- “还有哪些问题”“是否完成”等查询只读取 status 和已有证据，不主动重跑 check、构建或 audit；明确要求继续或重新验收时才执行相应步骤。
- 新增纯记录提交保持有效、记录与项目文件混合提交必须失效的回归测试。

## 0.5.8 — 2026-09-21

- `/dev` 无参数时进入当前会话、当前项目的开发模式；后续普通需求描述默认按 `next` 处理。
- 开发模式中可直接输入 `prd`、`status`、`check` 等裸命令词；`dev off` 退出。模式不写入项目，不跨会话或项目持久化。
- 明确保留 Claude Code 内置 `/status`，需求状态使用普通文字 `status` 或 `/dev status`；底层选择、记录和门禁规则不变。

## 0.5.7 — 2026-09-21

- 新增 `/dev next [自然语言描述] [--feature ID]` 日常统一入口：先读当前状态，再自动进入已有的 api/figma/clarify/revise/fix/develop/check 流程并实际执行。
- 图片按用途分流：预期效果或替换素材进设计来源流程，实际故障截图进 fix；只在分类会改变产品权威或记录类型时追问。
- 保留全部精确命令和现有门禁；`next` 不生成第二套状态，新需求仍由 `prd --feature` 创建。

## 0.5.6 — 2026-09-21

- 新建需求默认启用影响清单门禁，不再依赖 Agent 额外设置采用标记；旧工作区保持兼容。
- 已有模块目录的新工作区必须明确选择并复核模块，或记录不适用理由；develop/check 都会拒绝过期档案。
- intake 增加 `design` / `api` 来源类型；新增安全、幂等的来源登记工具，一次复制原始证据并同步 intake sources、`prd_paths` 及对应来源状态。
- 要求来源、需求、决策含义或任务语义改变后，在继续业务代码前重做必要复核并重跑 develop 校验。该顺序仍由 Agent 执行，尚无主机级代码写入拦截。

## 0.5.5 — 2026-09-20

- 归档/恢复/分类后报告共享记录的 Git 状态，区分未跟踪、未提交及被忽略的候选记录；重复归档也可查询保存状态。
- 报告当前分支和本地远端跟踪差异，明确未联网核验；不自动暂存、提交、推送或强制加入被忽略文件。

## 0.5.4 — 2026-09-20

- 增加项目模块目录和模块技术/功能档案，保存代码依据、核对版本及相关需求历史，生成可读视图。
- scan 支持 --module，按登记的目标模块、直接调用方和依赖核对；识别范围内新增/删除/修改与全局配置变化，无关提交不单独使模块档案过期。
- 新采用的需求在交付检查时核对模块档案；归档保留原需求，档案记录当前实现，不自动合并旧规则。自动依赖发现、精准行为影响传播与历史归档门禁解耦仍未实现。

## 0.5.3 — 2026-09-20

- check 接入验证列表：从需求验收条件、影响行为及已选质量命令生成条目；明确映射后自动执行，人工项保持待验收。
- 自动记录实际检查输出，人工结果要求实际观察与被测身份；保留失败历史和退役条目，代码/规则/映射变化使证据过期。
- 影响门禁复用列表结果，无需重复填写；不自动宣布 complete，不提供新的 UI 自动化驱动或完整依赖发现。

## 0.5.2 — 2026-09-20

- 增加 help 和 help <command>：分组展示命令说明与示例，单项说明参数、使用时机及相近命令区别。
- 帮助优先于项目初始化和需求选择处理，不扫描、不检查、不写记录、不执行示例；现有命令保持兼容。

## 0.5.1 — 2026-09-20

- 局部修改影响清单接入 develop/fix/revise 后的代码修改：调查相关机制，区分修改、保留和未决行为。
- check 核对 Git 实际范围及回归摘要；相关文件或清单变化使旧结果失效，支持明确排除与回归豁免。
- 旧需求不批量迁移，后续代码修改由 Agent 启用记录；不自动发现全部依赖或证明测试执行。

## 0.5.0 — 2026-09-20

- 第二阶段最小实现：可选语义需求基线、独立修订提案、批准依据与版本/原值冲突检查；保留原 PRD 和原始基线快照，当前规格继续复用 requirements.json。
- 确认版本与交付验证版本分别登记；质量检查可绑定 feature/version/digest，旧报告不证明新基线完成。增加修订视图及历史完整性/直接语义漂移检查。
- 复用现有决策、修复、任务、来源复核与完成门禁；不自动迁移旧需求，不实现模块现状索引、完整来源权威或自动影响传播。

## 0.4.11 — 2026-09-18

- 新增 archive/restore 和多模块 classify；归档原地保留目录及证据，记录验收版本、报告哈希和归档/恢复历史。
- list 默认展示活动需求，支持 --archived / --all / --module；状态与生成视图展示归档和模块。
- 未完成、未决或缺交付报告时拒绝归档；归档需求继续 develop/check/fix/clarify 前需恢复。
- 第一阶段轻量补强，不实现压缩删除、模块当前行为索引或版本自动传播。

## 0.4.10 — 2026-09-18

- 新增 list/use 会话内需求选择；只读 feature-context.py 核对项目、ID、标题及状态，显式 ID 仅覆盖本次调用。
- 成功完成新 PRD draft 流程后自动选择，失败/已有 ID 保留旧选择；多会话不共用磁盘指针。上下文丢失需重新选择。
- 更新流程图与命令说明；不改变业务状态、Git 分支、门禁或安装副本。

## 0.4.9 — 2026-09-18

- 根据八份独立 PRD 表达样例评估，新增 `review-prd.py --stage draft`：登记已读部分与待决/不可读缺口，不把尚未决议的条目改写成已映射。
- 完整 review 仍默认 develop 口径；draft 登记永不满足 develop/check。历史未标 stage 的完整复核保持兼容。单模型 8/8 样例语义结果及局限见验证记录；业务真机验收仍未完成。

## 0.4.8 — 2026-09-18

- 新增 `/dev clarify`，复用 decisions.json 登记原始疑问、调查证据、影响范围、确认依据与落实情况；支持 fix 共享决策。
- 新增登记脚本、状态摘要与可读视图；待决事项沿用开发/检查阻断，已确认未落实事项阻止最终 check；旧决策不强制迁移。
- 普通对话中的实质疑问在 Skill 已加载时要求留痕；尊重仅讨论要求。语义识别、影响分析与实际验证仍由 Agent 执行，不宣称自动捕获全部讨论或实现第二阶段。

## 0.4.7 — 2026-09-18

- 补齐运行契约的实际字段/状态说明及最小示例，不把浅层 JSON Schema 说成完整实现。
- 质量检查选项可标记本次实际覆盖的测试 ID；新增只读 `audit-delivery.py` 对照已验证修复的回归 ID、已通过的检查结果与当前项目快照，提示旧报告和漏选测试。
- 任务内容、决策范围、断言真实性、未登记源码的未提交变化及人工交付报告仍需 Agent 核对；不自动修改业务记录或宣布 complete。

## 0.4.6 — 2026-09-18

- 修复 GAP-01：回归测试默认须属于本条修复的受影响需求或关联任务；跨范围已登记测试须逐项说明覆盖原因，豁免不允许绕过无效引用。
- 补齐 GAP-02：新增 closed 处置，区分有证据的非缺陷、经确认撤回和重复报告；重复项直接关联原报告，原问题未解决仍阻止功能检查。
- 同步状态摘要、生成视图和再次报障登记；closed 不算 verified，不自动恢复 complete。已有 verified 的范围不合规引用升级后需核对补齐，不自动改业务记录。
- 合成回归验证不证明关闭结论真实或测试实际覆盖；历史修复重验、任务语义一致性及最终报告同步仍未实现。

## 0.4.5 — 2026-09-17

- 新增 `/dev fix` 问题登记入口、规范化 fixes.json 与自动生成 fixes.md；已完成的功能报告新问题时回到 provisional。
- 区分代码缺陷、原文误读、需求变更、来源冲突和环境问题；需求变更/冲突关闭需关联已确认决策。
- 未验证的问题阻止功能级 check；修复完成要求实际复现与测试证据及测试版本。支持待查后记录多重成因和子任务描述前后变化，关闭时关联回归测试或明确豁免。旧工作区无 fixes.json 时保持兼容。

## 0.4.4 — 2026-09-17

- PRD 初始化支持单文档、单 HTML 和多个文档/HTML 组合，保留旧单文件调用。
- intake 登记全部来源，校验各来源阅读单元及已观察交互字段；文件字节变化使复核过期。
- 明确 HTML 动态依赖、示例数据和多来源冲突的人工复核边界。

## 0.4.3 — 2026-09-17

- 新增业务项目选择性提交规则、独立 .gitignore 模板及安装脚本；保留既有规则，提示已跟踪文件和父级忽略冲突。
- 原始来源默认忽略，明确逐项共享与恢复依赖；新克隆保留共享分析、复核后建立本机指纹。
- 不自动暂存、取消跟踪、提交、推送或重写历史。

## 0.4.2 — 2026-09-17

- 补齐维护交接入口、已确认架构决策、真实验证启动条件；区分源码、测试、安装与远端发布状态，修正 README 版本。

- 扫描草稿与发布分离，变更才备份，默认保留最近 3 份托管快照；支持保留数量与固定快照。
- 发布前结构/配置检查、并发变更检测、异常回退与中断恢复；不清理旧手工备份和功能记录。
- 无变化保持正式文件与修改时间；分析语义仍由 Agent 复核。

## 0.4.1 — 2026-09-17

- 修复真实 scan 验证暴露的配置契约问题：validate-gates 只校验不执行，拒绝字符串命令、未知条件字段和未填模板参数。
- 区分候选检查与本次执行清单；空选择不冒充质量通过。
- 增加模块事实交叉核对、规则例外与覆盖范围记录；v2 指纹纳入实际登记证据。
- 保留完整增量扫描和任务级影响分析为后续工作。

## 0.4.0 — 2026-09-16

- 新增 PRD 读取清单、上下文条目、条件/例外覆盖及生成分析视图。
- 新增结构与复核新鲜度检查；缺失 intake 的旧工作区需补齐才能继续 develop/check，draft 可查看提示。
- 增加归因复核命令和多表达/细微语义差异样例；脚本不代替语义理解，未执行跨模型准确率评估。
- 同步 Skill、指南、流程图与交接状态。

- 在使用与迭代指南中补充当前日常工作流程图，标明可选资料、后补变更、冲突决策、质量检查与完成条件；增加流程变更时同步维护图示的约定。

## 0.3.0 — 2026-09-16

- 从 Android 版本拆出通用 core 与 Android / Generic profiles。
- 单份 skills/dev 支持 Claude Code /dev 与 Codex $dev；安装器提供安装、更新、可恢复卸载和冲突检测。
- 增加可审查质量命令、清单新增/删除失效检测、不适用来源及理由。
- 修正空工作区开发放行、迁移覆盖已有 JSON 和初始化源文件扩展名问题。
- 补齐 README、使用指南、维护交接、架构决策、路线图与隔离测试。
- 兼容：JSON 路径不变；旧指纹需重扫；YAML 仍须人工/Agent 重建。旧安装保持原状，迁移步骤见 README。

## Legacy — 未标版本

原 android-feature-delivery 最小实现，包含基本 JSON 校验、渲染、状态摘要和 Android 关键词扫描。此前无版本文件，不推断或补造旧版本号。
