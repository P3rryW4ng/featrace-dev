# FeatraceDev 项目维护总览与交接

## 文档名称与分工

本仓库中管理 Skill 目标、方向、进度、决策和验证的材料统称 **Skill 项目治理与维护文档**，日常简称 **项目维护文档**。本文件是统一入口；不另建重复的“当前进度表”。

| 日常称呼 | 文件 | 负责回答 |
|---|---|---|
| 产品方向基线 | [PRODUCT.md](PRODUCT.md) | 已确认的当前阶段目标、战略北极星、最终愿景、核心边界与成功指标 |
| 项目维护总览与交接 | 本文件 | 目标是什么、当前做到了什么、有哪些限制、如何接手 |
| 开发路线图与阶段计划 | [ROADMAP.md](ROADMAP.md) | 各阶段进度、下一优先项、启动与完成条件、待处理问题 |
| 架构与产品决策记录 | [architecture.md](docs/decisions/architecture.md) | 为什么这样设计、哪些原则已确认、哪些实现仍待验证 |
| 验证与评估记录 | [validation.md](docs/validation.md) | 实际验证了什么、结果和证据是什么、哪些未验证 |
| 阶段审查报告 | [阶段审查](docs/reviews/2026-09-17.md)、[真实案例 CASE-01](docs/reviews/2026-09-18-case-01.md)、[真实案例 CASE-02](docs/reviews/2026-09-21-case-02.md)、[OGFR-2577 独立复核](docs/reviews/2026-09-24-ogfr2577-independent-verify.md)、[Android 模块候选只读核对](docs/reviews/2026-09-24-android-static-candidate-real-check.md)、[0.5.11 隔离演练](docs/reviews/2026-09-21-historical-regression-isolation.md)、[0.5.13 隔离演练](docs/reviews/2026-09-22-task-semantic-review-isolation.md)、[GAP-05 隔离测量](docs/reviews/2026-09-22-scan-granularity-isolation.md)、[GAP-05 Agent 复检](docs/reviews/2026-09-22-scan-diagnostics-agent-isolation.md) | 某个时点发现的问题及复现依据；当前处理状态以路线图为准 |
| 使用与迭代指南 | [usage.md](docs/usage.md) | 使用者如何操作当前版本、流程和限制是什么 |
| 版本变更记录 | [CHANGELOG.md](CHANGELOG.md) | 每个版本发生了哪些变化；不代替当前进度与远端发布核对 |
| 仓库维护规则 | [AGENTS.md](AGENTS.md) | 维护者和 Agent 修改本仓库时遵守什么规则 |

[README.md](README.md) 负责项目介绍与安装导航。[skills/dev/SKILL.md](skills/dev/SKILL.md) 及其 references、脚本属于 **Skill 运行规范与实现**，会随安装分发。项目维护文档留在仓库；修改路线图不会自动改变已安装 Skill 的行为。业务项目生成的需求、任务、修复记录属于 **需求交付工作区文档**，与本表的 Skill 维护文档分开。

每项信息只设一个主要维护位置：产品使命、目标用户与成功标准看 PRODUCT，能力与限制看本文件，优先级与问题状态看路线图，依据看验证/审查记录。发生变化时更新主要位置与必要链接，避免在多份文件复制完整待办。历史审查保留当时结论，不能把后来修复伪装成当时没有问题。

## 长期目标

长期方向以 [PRODUCT.md](PRODUCT.md) 为准：当前先增强开发者并减少返工、遗漏和黑盒交付；战略上把持续变化的产品意图转成可验证、可接续的变更；最终在明确边界内提高自主交付比例。当前事实与证据能力是安全底座，不是最终产品的全部。成功标准是实际交付更自主、更准确、可追溯且成本可接受；字段数量、文件数量或结构校验通过率不能单独代表成功。

## 当前目标与版本

产品名 **FeatraceDev**，仓库名 `featrace-dev`，本地路径 `/Users/mac/Entrepôt/featrace-dev`。品牌理念为 Feature · Trace · Delivery，作者署名为 Created by Perry Wang。0.5.22 完成品牌与双语 README 迁移；运行入口仍为 dev，安装器兼容旧身份。本次未更新个人安装副本，现有 0.5.21 功能可继续使用。

当前源码 [VERSION](VERSION) 为 **0.5.22**，本机 Claude Code 安装版仍为 **0.5.21**；本次交接核对日期 **2026-09-24**。第一阶段已完成；第二阶段已实现最小语义修订、经 Agent 审阅的模块档案、业务能力与多模块职责确认，以及 Android 静态 Gradle 模块/依赖候选。0.5.17 开始在单一入口内部按六个专业阶段组织规则与交接；当前仍是同一 Agent 的渐进加载，不是多 Agent 隔离。候选只缩小人工调查范围，不自动成为正式模块事实。完整来源权威、动态运行时调用图和自动影响传播尚未实现。运行 Skill 名为 dev；唯一源码权威是 skills/dev，安装目录只是副本。

0.5.18 修复追溯 Markdown 渲染：旧生成器漏读 `links[].tasks`。独立复核 OGFR-2577 确认共享切图消费者遗漏，也出现了两处与隔离副本事实不符的断言；用户后续目视查看未见异常。暂不改变该业务需求的归档/验收结论，不据此启动六 Skill 拆分。依据与局限见[本次审查](docs/reviews/2026-09-24-ogfr2577-independent-verify.md)。下一轮 Verify 对照按[试验协议](docs/evaluations/verify-comparison-protocol.md)执行。

0.5.19 在既有 Verify 阶段加入只读输入核对：明确列出可读来源、补充文件、工作树变化与质量报告的记录版本；不新增日常命令、记录格式或验收通过条件。参见[验证记录](docs/validation.md)；发布与安装状态见下方实际核对。

0.5.19 目标测试 **8/8**、完整合成回归 **220/220 通过**（34.460 秒）；目标测试覆盖来源索引兼容与符号链接拒绝。OGFR-2577 隔离副本只读核对发现两份索引来源与一份补充图片均可读，同时提示报告登记版本早于当前 HEAD。合成纵向演练从失败测试走到修复复测，并发现、修正旧版 intake 的误报；这验证了盘点和脚本链路，尚未证明独立 Agent 的判断质量或完整自主开发。详见[验证记录](docs/validation.md)。

本轮在另一真实 Android 项目的构建声明隔离副本上核对静态候选：直接声明的 30 个模块全被发现，但外部 settings 脚本动态声明的 16 个模块均漏掉，且脚本加载本身未被缺口提示。已按[只读核对记录](docs/reviews/2026-09-24-android-static-candidate-real-check.md)保留证据并在 GAP-09 登记最小修正；没有修改业务项目、运行 Gradle 或更新安装版。

0.5.20 已按该记录补两类行号提示：外部 settings 加载与 `add` 形式的项目依赖。同一隔离副本复跑仅使解析缺口从 19 增至 21，模块 30 和候选边 166 保持不变；它提醒人工调查，不自动补齐 16 个动态模块。目标回归 19/19、完整合成回归 222/222 通过（34.675 秒）；实际范围及限制见[验证记录](docs/validation.md)；功能提交 `2444322` 已推送，Claude Code 安装副本已更新；实际安装核对见下方发布状态。

随后已安装 Claude Code 在明确提示及普通 `/dev scan` 两种隔离请求中都追查了外部 settings 与动态依赖；普通请求的 Agent 也在读取候选前直接打开外部脚本，因此不能把效果完全归因于新提示。普通演练发现图读取器会因未入静态候选的目标边拒绝整份候选，导致图丢失所有待审边与缺口；已在 [GAP-18](ROADMAP.md) 登记。Agent 还把条件依赖写成无条件正式边、从构建注释提升模块职责，真实目录采用前要人工审阅。证据见[普通请求演练](docs/reviews/2026-09-24-natural-scope-rehearsal.md)；该轮演练未修改 Skill 运行代码或重新安装。

GAP-18 已在 0.5.21 源码修复：未知目标以待审节点和虚线边进入候选图与正式关系图，行号缺口不丢失；损坏记录仍拒绝。同一隔离 Gradle 声明副本复测原 17 条未知目标边保留。随后针对 GAP-19 增加最小保护：可识别的变体专属依赖只留条件候选；若正式目录把它写成无条件依赖，关系图/扫描发布拒绝。隔离副本已验证拒绝及修正后保留虚线条件边。功能提交 `45d74ca` 已推送至 `origin/main`；本机 Claude Code 安装版 0.5.21 的 67 个 Skill 文件与源码哈希一致，旧版备份位于 `/Users/mac/.feature-delivery/backups/20260924T065712-claude-690c4678`。动态语法覆盖、Agent 实际纠正行为和构建注释升格仍需继续观察；不以这些结构修正替代真实需求交付验证。

### 当前发布状态（2026-09-24）

- 0.5.20 功能提交 `2444322` 已推送到 `origin/main`。本机 Claude Code 安装标记为 0.5.20，源码与安装目录的 67 个 Skill 文件逐一一致，仅多安装标记。0.5.19 的备份为 `/Users/mac/.feature-delivery/backups/20260924T053436-claude-10422667`；发布文案同步后的中间副本备份为 `/Users/mac/.feature-delivery/backups/20260924T053526-claude-a45e7ad1`。完整合成回归 222/222 通过；动态模块仍需 Agent 阅读提示并人工确认。

- 0.5.19 功能提交 `a6bb9a1` 已推送到 `origin/main`，本机 Claude Code 安装标记为 0.5.19；源码与安装目录的 67 个 Skill 文件逐一一致，安装目录仅多安装标记。旧版备份：`/Users/mac/.feature-delivery/backups/20260924T044628-claude-3b09ac59`。完整合成回归 220/220 通过；下一次真实需求仍需验证 Agent 是否实际执行输入核对，以及遗漏、耗时和上下文成本。

- 0.5.18 修复追溯 Markdown 任务显示，完整合成回归 212/212 通过（34.341 秒）。本机 Claude Code 安装副本与源码 66 个文件哈希一致；旧版备份为 `/Users/mac/.feature-delivery/backups/20260924T031154-claude-cab433f1`。已建立下一轮独立 Verify [对照协议](docs/evaluations/verify-comparison-protocol.md)，尚未执行严格双组对照。

- 0.5.17 功能提交 `5b2a21a`；本机 Claude Code 已更新到 0.5.17，安装副本与源码 66 个文件哈希一致，旧版备份为 `/Users/mac/.feature-delivery/backups/20260923T074003-claude-d6c0181b`。主 Skill 入口从约 2613 词缩至 721 词，实际上下文与时间收益尚待真实需求测量。完整合成回归本轮重跑 211/211 通过（34.691 秒），本地链接与差异检查通过。临时插件中的独立 Claude Code 新会话完成六类文字路由演练：首次自动评分 5/6，唯一失败是无项目归属情况下的评分标准歧义；修订标准并全新复跑该例通过，工具轨迹显示正确加载候选 Skill 与对应参考文件。它不证明实际项目写入或开发闭环。通用 quick validator 因本机缺 PyYAML 未运行成功。远端同步以 Git 为准。

- 0.5.15 功能提交为 `77dbb32`，已推送到 `origin/main`；模块、归档与基础工作流目标测试 40/40、完整合成回归 207/207 通过。本机 Claude Code 已安装 0.5.15，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件；备份为 `/Users/mac/.feature-delivery/backups/20260922T093854-claude-b5355388`。尚未执行普通 Agent 跨模块范围确认演练或真实项目关系图评估。
- 0.5.14 功能提交为 `7f3cca1`，已推送到 `origin/main`；扫描诊断与备份目标测试 26/26、完整合成回归 202/202 通过。本机 Claude Code 已安装 0.5.14，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件；备份为 `/Users/mac/.feature-delivery/backups/20260922T080154-claude-8c64c38c`。三步 Agent 隔离复检覆盖未登记工作树变更、HEAD-only 局部刷新和 workflow-only HEAD 放行，全部符合预期；真实 scan 阅读时间与语义判断成本仍待测量。
- 0.5.13 功能提交为 `f360a8e`，已推送到 `origin/main`；任务语义目标回归 9/9、完整合成回归 194/194 通过。本机 Claude Code 已安装 0.5.13，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件；备份为 `/Users/mac/.feature-delivery/backups/20260922T052943-claude-85aefbb8`。四步 Agent 隔离复检覆盖进度不失效、冲突阻断、局部复核、未批准中间态不留史及历史防篡改，全部符合预期；真实成本留待下一需求测量。
- 0.5.12 功能提交为 `9e342f6`，目标回归 9/9、完整合成回归 185/185 通过；已安装到本机 Claude Code，备份为 `/Users/mac/.feature-delivery/backups/20260922T024117-claude-85af2f6b`。隔离复检覆盖只同步保持 pending、changed/removed 留史、旧 passed 不放行、v1 迁移、幂等、状态展示和历史防篡改，全部符合预期；真实候选准确率与成本留待下一需求统一验证。

- 0.5.11 功能提交为 `27278f4`，已推送到 `origin/main`；本机 Claude Code 安装标记为 0.5.11。合成回归 180/180 通过；隔离 Agent 演练确认机制链路与自动触发，但发现明确不代选仍写入 retest、失效结果不在记录内留史两项缺口。真实候选准确率与成本仍未验证。
- 0.5.10 功能提交为 `362daecefc53599be4f2b8d36f5ff2f229216495`，已推送到 `origin/main`；本机 Claude Code 安装标记为 0.5.10，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件。旧版备份：`/Users/mac/.feature-delivery/backups/20260921T081847-claude-a33fc59f`。
- 0.5.9 功能提交为 `833903c938d81e47d0758546274f131d311a0386`，已推送到 `origin/main`；本机 Claude Code 安装标记为 0.5.9，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件。旧版备份：`/Users/mac/.feature-delivery/backups/20260921T073254-claude-3bd23ff7`。安装版对 OGFR-2577 只读复查返回 `DELIVERY_EVIDENCE_CURRENT`。
- 0.5.8 功能提交为 `2a8a57c2f89ac2d8bf48d2cffa2a979ca88d2c4c`，已推送到 `origin/main`；本机 Claude Code 安装标记为 0.5.8，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件。旧版备份：`/Users/mac/.feature-delivery/backups/20260921T070243-claude-259de811`。本机 Codex 当前没有 `~/.codex/skills/dev` 安装标记，未自动同步。
- 0.5.7 功能提交为 `3f79896ccd710043b656f4f02f4fd5967eede0eb`，已推送到 `origin/main`；本机 Claude Code 安装标记为 0.5.7，安装内容与仓库 `skills/dev` 一致，仅多安装标记文件。旧版备份：`/Users/mac/.feature-delivery/backups/20260921T065305-claude-7789b016`。
- 0.5.6 功能提交为 `2cf5d40fe2e6519ff13eb78d11e42317edfe842c`，已推送到 `origin/main`；发布状态文档同步为后续提交。
- 0.5.6 发布前完整合成回归为 172/172 通过；这不是业务 Android 或真机验证。
- 0.5.7 仅改变 Agent 指令与文档，静态一致性和链接检查通过；没有把 172/172 描述成重新执行。
- 新会话加载最新 Skill；其他机器、账号和旧会话不会自动更新。通用 `quick_validate.py` 仍因本机缺少 PyYAML 未执行成功，不记为通过。

0.4.10 增加 list/use 和成功新建后选择；只读 helper 验证目标，会话默认选择由 Agent 上下文维护，丢失后需重新选择，未提供主机级持久会话隔离。本次版本包含此前 0.4.9 的 draft 评审修正；提交、远端与安装状态分别以 Git 和安装标记核对，不能仅凭源码版本推断。

0.4.11 实现原地归档/恢复、多模块标签与列表过滤；归档保留历史，不证明旧规则仍生效。安装副本需单独核验，本轮影响清单实现不自动更新安装。

0.5.0 当前开发范围为已确认的最小修订方案：复用当前 JSON、单独记录差异与依据、版本/原值冲突检查、区分确认与验收版本。元数据可检测直接语义漂移，任务/代码影响仍由 Agent 核对。维护文档前置提交为 d6433ba；源码提交、推送和个人安装状态需分别按 Git 与安装标记核对。

### 接手先确认

1. 读取本文件、[路线图](ROADMAP.md)、[架构决策](docs/decisions/architecture.md)、[验证记录](docs/validation.md) 和 `skills/dev/SKILL.md`。
2. 查看 `VERSION`、`git status --short`、最近提交及本地与远端差异，保留已有未提交修改。版本号或 CHANGELOG 条目不能证明已经提交、推送或发布。
3. 最近已推送提交及当前远端状态以 `git status -sb`、`git log` 和 `origin/main` 实际结果为准，不以版本号或本文件推断。
4. 2026-09-17 真实案例校对时已检查本机 Claude 安装标记及 34 个登记文件，确认与当时 0.4.5 源码一致；不能据此推断历史每次调用版本或后续安装状态。仓库源码更新后按 README 的安装流程更新各机器的个人安装副本，并核对安装标记及文件哈希；其他机器和账号不会自动同步。
5. 把用户当前请求映射到已实现能力或路线图，先核对代码/测试，再决定修改范围。仓库内容与聊天描述冲突时，记录差异并核实，不凭旧对话补造能力。

## 已实现

- 0.5.16 在 Android scan 中解析常见静态 Gradle 模块和 `project(...)` 依赖，生成带文件/行号依据、文件哈希、正式目录匹配和解析缺口的候选 JSON/Markdown。正式图以虚线展示候选；脚本不修改人工目录、不确认业务归属、不解析动态 Gradle 或运行时调用。合成测试覆盖 Kotlin/Groovy、projectDir、未知目标、动态表达式、scan 自动生成及旧图兼容；真实 Android scan 待验证。

- 0.5.15 将产品编号、业务能力和代码模块分开。新需求使用用户提供的产品/work-item ID，develop/check 前必须确认 capability 与每个相关模块的 owner/host/provider/consumer/shared 职责及依据，或说明不适用；路径匹配只提供候选。模块目录生成 JSON 与 Mermaid 关系图，展示声明依赖和已确认职责，不宣称完整运行时调用图。目标与完整合成回归通过，真实跨模块需求成本待验证。

- 0.5.11 在 develop/fix/revise 的业务代码修改前同步当前及跨需求的历史 `verified` FIX 候选，按当前需求模块和登记代码路径缩小范围；每项必须选择重测或说明不适用。check 要求重测结果绑定当前候选摘要、方法、证据和版本；范围或历史 FIX 变化后旧结论失效。旧 FIX 不被自动重开，脚本也不声称发现未登记依赖。目标回归 4/4、完整合成回归 180/180 通过；隔离演练的确定性链路通过，Agent 控制边界部分通过。

- 0.5.10 `/dev archive` 先运行预检：已有报告不覆盖，缺失/空报告从结构化记录生成带固定待审标记的草稿；正式归档拒绝未审草稿。预检报告模块候选、代码路径根、impact、结构/audit 结果和 sources Git 状态，但不自动分类、上传或声称验收。目标归档回归 11/11、完整合成回归 176/176 通过。

- 0.5.9 audit 对检查 HEAD 与当前 HEAD 做路径差异核对：纯 `.agent-workflow/**` 收口提交不使质量证据失效，混入其他跟踪文件则仍过期；只读进度问题不再自动重跑验证。174/174 合成回归通过，0.5.9 源码与 Claude 安装版对真实 OGFR-2577 只读复查均返回 DELIVERY_EVIDENCE_CURRENT。

- 0.5.8 无参数 `/dev` 激活当前会话和项目的开发模式；普通交付描述默认走 next，裸命令词复用现有流程，`dev off` 退出。它不写持久状态，不跨会话或项目，不覆盖 Claude Code 内置 `/status`。真实会话保持与误触发率待验证。

- 0.5.7 `/dev next` 读取已选需求状态和自然语言描述，说明并执行既有资料、澄清、修订、修复、开发或验收流程。它不另建状态或跳过门禁；图片按预期素材或故障证据分流，新需求仍由 `prd --feature` 创建。真实 Agent 路由准确性和节省成本待下一需求验证。

- 0.5.4 项目模块目录及每模块技术、功能、入口、状态生命周期、测试和历史需求引用；模块计划核对登记范围文件与共享配置，相关字节变化使档案过期，无关提交不单独失效。Agent 解释代码和维护依赖声明，脚本不会自动发现遗漏关系。scan --module 进入局部流程；原全局质量检查仍保留构建配置遍历。

- 0.5.3 自动生成 verification.json/视图并保留历次结果，自动项复用项目质量执行器，人工项明确观察、证据与被测版本；影响门禁直接读取列表结果。旧需求不批量迁移，继续验收时由 Agent 采用。未提供设备/UI 驱动；自动覆盖映射仍需审阅断言，摘要按整个列表保守失效，不保证完整源码或外部环境变化检测，也不自动 complete。

- 0.4.9 为 PRD 待决/不可读内容增加 draft 局部复核登记；不能冒充完整复核。8 份合成表达样例语义评估已由执行方完成，单模型一次运行，原件与适用边界见验证记录。真实案例最终版本已与报告及 complete 状态对齐；最终代码与记录提交、证据边界见验证记录。

- 0.4.8 `/dev clarify` 复用 decisions.json，登记、调查与落实跟踪；旧决策兼容。普通对话留痕依赖 Skill 已加载与 Agent 识别；来源解释、影响范围和证据真假仍需核对。已确认未落实事项阻止 check，不等于自动影响传播。

- `/dev fix` 在现有功能中登记问题，保存结构化修复记录并生成 fixes.md；开发阶段警告未关闭问题，功能级 check 阻止未验证且未有效关闭的问题。初始原因可待查，支持子任务描述缺口与实现缺陷并存；验证修复需关联受影响需求/任务的回归测试，跨范围逐项说明原因，或明确豁免；非缺陷、经确认撤回和重复报告可有据关闭，重复项不绕过原问题。分类与修复结论仍由 Agent 核对证据。

- PRD 单文档/单 HTML/多文件组合初始化、来源清单与交互观察字段校验；动态浏览器操作与冲突判断仍由 Agent 完成，尚无自动遍历或完整性保证。

- 业务项目选择性 Git 提交规则、独立忽略模板与幂等安装；报告已有跟踪/父规则冲突，不自动改索引。新克隆复用共享笔记，复核后重建本机指纹。原始来源默认忽略，来源恢复与共享权限仍需核对。

- 扫描草稿/发布、默认 3 份托管备份、无变化不重写、固定快照、异常回退。具体故障边界见 [扫描契约](skills/dev/core/references/project-baseline.md)。

- scan 配置纯校验、候选/执行清单分离、作用域与例外/事实核对规则、覆盖清单。
- 指纹 v2 纳入根规则和显式注册证据；不是完整 Git 增量扫描。

- PRD 统一分析规范：读取清单/缺口、原文上下文、关系、条件与例外双向覆盖。
- intake v1、可读分析视图和开发前门禁；来源/需求改变使复核失效。复核命令只记录已经进行的人工或 Agent 检查。
- 多表达与细微语义差异样例；行为评估协议，未宣称已验证模型准确率。

- 通用 core 与 android/generic profiles，未创建空的 iOS/Web 适配目录。
- Claude /dev、Codex $dev；一次安装脚本，校验安装归属和本地修改，更新/卸载保留备份。
- JSON 需求/任务/决策/追溯；基本结构检查、生成 Markdown、状态摘要。
- 清单指纹（含新增/删除）、通用显式质量命令与结果记录。
- 来源不适用可明确记录；空需求/迁移未完阻止开发；迁移拒绝覆盖已存在 JSON。

## 不可破坏原则

原始证据不覆盖；推断不伪装成事实；一手来源冲突保留人工决策；权威按字段；JSON 是编辑源，Markdown 是视图；项目记忆与 Skill 本体分离；不适用的资料不能强制要求。

## 真实限制

- intake 校验无法发现从未枚举的原文、虚构摘录或语义误判；需要回读原文。旧 0.3 工作区继续开发前须补齐 intake，不能自动视为已复核。
- PRD 复核 digest 仍是本地防过期机制；语义基线和模块目录均需 Agent 核对，尚无完整来源权威或自动影响分析。模块档案表示当前审阅的实现，不自动裁决历史规则的替代关系。
- 影响清单登记入口、调用方、共享状态、导航、生命周期和旧行为，脚本检查未决项、实际 Git 范围及内容绑定的回归结果。0.5.6 新建工作区默认要求它；旧需求继续改代码前仍由 Agent 启用，不批量迁移。脚本不能发现遗漏依赖、证明证据真实或拦截 Skill 外的任意代码写入。共享摘要可能保守扩大重验范围，真实成本待评估。

- 0.4.6 的修复测试关联和非缺陷关闭只验证记录契约；跨范围覆盖理由、关闭证据和实际测试结果仍需 Agent 复核，不能证明结论真实。旧 verified 的跨范围引用可能需要补充核对，安装更新不会自动改业务记录。
- 已验证修复现在可按已登记模块/路径进入当前需求的候选复核，但不会自动执行测试，也不能发现未登记依赖；PRD 复核摘要仍不覆盖子任务描述，任务含义改变不自动触发复核。相关能力边界见 GAP-03/04；不能把历史 verified 当成本次回归通过。
- 0.5.12 已规定只同步/列出/停止时保持 pending；这属于 Agent 行为约束，脚本无法读取会话意图，仍需短会话复检。候选变化或移除后的旧 disposition/result 现保存在只读 history 中，不会满足当前门禁。见 GAP-16/17。
- schemas 仍是浅层结构说明，校验器未执行完整 JSON Schema。合法 test ID 不证明测试存在或执行。
- 决策审批、双向追溯、完成状态一致性等语义约束仍不完整。不要宣称“强状态机已完成”。
- develop 校验仍在有任何 blocked 需求时阻断整个功能，尚无任务级 eligibility。
- YAML 迁移仅创建脚手架，依赖 Agent 按旧记录重建，不是自动转换。
- PRD/API/Figma 分析主要由 Agent 完成；无 OpenAPI 自动解析或 Figma 连接器。HTML 交互需要实际可用的浏览器/运行环境，脚本不执行或穷举交互；外部依赖可能使单个 HTML 文件无法复现。
- 扫描只盘点清单，不自动理解架构；指纹不覆盖未登记源文件的未提交变更；质量报告不是完整交付证明。
- 0.4.7 已补充主要字段/状态说明并提供只读交付核对；但检查项的 `test_ids` 仍由 Agent 如实标注，任务/决策含义、断言范围、未登记源码变化和交付报告仍须人工核对。见 GAP-07/08。
- 质量配置 config.yaml 是 Agent 指导；实际命令来自 quality-gates.json，二者没有自动策略同步。
- 仓库自动测试使用合成项目与模拟构建；CASE-01 的真实 Android 构建与真机结果由执行方提供，维护者核对记录，未独立重跑。不能据此认为 0.4.10/0.4.11 新入口已完成真实 GUI 会话验证。安装状态不应从此文推断；核对各机器安装标记。

## 扫描维护约定

- Agent 的 `/dev scan`：`scan-prepare` → 在草稿中复核与修改 → `scan-publish` → `verify` / `validate-gates`。低层 `scan` 仅兼容清单刷新，不能代替 Agent 分析。旧 Android `scan-project.sh` 会直接覆盖文件，不作为正常入口。
- 正式基线固定一份，变化才保存完整旧版，默认保留 3 份未固定的托管备份。`PINNED` 额外保留；`--keep` 只对该次调用生效，不是已持久化的项目配置。
- 清理仅作用于新机制管理的扫描备份；旧手工备份、安装备份、PRD/任务/决策历史不在范围内。无变化不新建备份，不意味着没有执行清单遍历。
- 发布检查文件/配置结构与已覆盖的变更，不判断语义正确性。覆盖范围、规则例外和新增证据仍需 Agent 复核。目录替换有短暂间隙；异常回退和下一次扫描恢复不等于断电持久性或全局原子事务。

## 当前交接重点

2026-09-17 对 bd0cfc8 的阶段审查已完成：方向仍符合目标；需要防止记录复杂度增长快于实际交付验证。优先补齐修复测试关联与非缺陷关闭规则，再以轻量方式试行历史修复回看和任务内容核对。其中 GAP-01/02 已在 0.4.6 完成契约补强，GAP-07/08 的确定性部分已在 0.4.7 实现，其余状态见路线图。详细顺序、问题编号与验收条件统一维护在 [路线图](ROADMAP.md)。

已有两个真实需求反馈。CASE-02 验证了“一句话 + Figma”场景中多来源对齐的价值，也暴露了设计来源类型、来源路径双写、develop 前置项延后补齐及新能力未采用的问题。本次没有使用 `module_context`，impact checklist 也未在首次代码修改前建立，所以两项能力的真实效果仍待验证。

下一步先处理可重现的契约和采用缺口，再用一次真实局部修改显式运行模块档案与影响清单，采集阅读范围、耗时、遗漏和误阻塞。同时继续验证第二阶段最小修订流程，并对归档/切换等入口收集真实反馈。合成回归通过不证明语义准确率、子任务一致性或防复发能力；证据与未验证范围见 [验证记录](docs/validation.md)。

## 换会话或换机的接续点（2026-09-23）

第一阶段已完成；剩余可靠性问题保留开放状态。0.5.17 的单入口六阶段契约已提交并安装到本机 Claude Code；本机 Codex 尚未安装该 Skill。下一真实跨模块需求除原有范围推荐、历史候选和遗漏成本外，还要记录阶段路由是否正确、重复加载、交接遗漏、非必要人工介入与总耗时；这些数据决定是否隔离 Verify。动态依赖发现、规则取代和传播仍待后续范围确认。

先读 [CASE-01 脱敏校对](docs/reviews/2026-09-18-case-01.md)，再按路线图接续。审查与资料归档已完成；GAP-01/02 已在 0.4.6 源码实现并通过合成回归，真实使用效果仍待验证。GAP-03 的确定性提醒已在 0.5.11 发布，GAP-07/08 的确定性部分已在 0.4.7 实现；首个真实项目已最终验收，但该案例通过不证明所有记录语义可靠。GAP-04 与局部影响面继续按真实案例试行；提交、推送和安装状态分别以 Git 与各机器安装标记核对，不能从版本号推断。

CASE-01 最终代码 14bc14fac、记录 1cc96db99：complete 与报告一致。完整 V2 证据来自旧诊断包，最终包另有 M2/M12 冒烟；M3 目视、FIX-005 自动化 waiver、FIX-007 旧观察保留均属明确边界。维护者仅核对记录与代码差异，没有代替用户重做真机。后续按相关改动选择回归，不把一次完成当永久正确。本仓库只存脱敏结论；原验收包仍在所有者本地，跨机不可假设原件随 Git 提供。

每次交接区分已发现、计划中、已实现、已验证，并记录对应证据。提交和推送状态以 Git 核对，未推送修改不会随另一台机器的 clone 出现。

## 验证

仓库根目录执行 `python3 -m unittest discover -s tests -v`。安装测试使用临时 home，业务测试使用临时项目；不触及个人配置。每次改动还需核对 Skill 内相对路径、文档命令和来源适用性规则。

## 交接收尾

行为变更时同步维护能力边界、决策、验证证据和使用流程图；记录“实际做了什么、没有验证什么、下一步启动条件”。提交、推送、安装是三个不同动作，分别报告实际结果。0.4.5 增加修复记录，0.4.6 补齐关联校验与非缺陷处置，0.4.7 补齐契约说明与只读交付核对；实际回归数量与缺口见 [验证记录](docs/validation.md)。

仓库不保存真实项目路径、业务 PRD、私有代码或个人配置；真实反馈先脱敏。聊天分享链接是背景资料，不能替代本文件和可执行实现。
